# Plus aucun secret cote client depuis la v1.6.2.
# L'export Google Drive utilise chrome.identity.getAuthToken :
# le client OAuth (type "Chrome Extension") et les scopes sont declares
# dans public/manifest.json (bloc "oauth2"). Rien de sensible dans le bundle.
