# v1.6.2 — Export Google Drive sans secret (getAuthToken)

## Ce qui est fait (code, commit `67981e6`)
- `src/lib/drive-export.ts` : l'auth Drive passe par `chrome.identity.getAuthToken`.
  Plus aucun `client_id`/`client_secret` dans le code → rien de sensible dans le bundle.
  Vérifié : `dist` reconstruit contient 0 occurrence de `GOCSPX`/`client_secret`.
- `public/manifest.json` : bloc `oauth2` ajouté (scope `drive.file`), version 1.6.2.
- `dist/` retiré du suivi git (il avait été committé par erreur en v1.6.1 AVEC le secret).

## Ce qu'il reste à faire (Brice) — l'export Drive ne marchera qu'après

### 1. Créer le client OAuth "Chrome Extension" dans Google Cloud
- Console → APIs & Services → **Identifiants** → Créer des identifiants → **ID client OAuth**.
- Type d'application : **Extension Chrome** (Chrome App/Extension).
- **ID de l'extension** : `klggbgjkojkcblgodajdjhhineocchbe`
  (calculé depuis la `key` du manifest — c'est l'ID stable de l'extension).
- Copier le nouveau `client_id`.

### 2. Renseigner le client_id dans le manifest
- `public/manifest.json` → remplacer `__REPLACE_WITH_CHROME_EXTENSION_OAUTH_CLIENT_ID__`
  par le nouveau client_id (format `xxx.apps.googleusercontent.com`).
- Vérifier que l'écran de consentement OAuth autorise le scope `drive.file`
  et que le compte est test user (ou app publiée).

### 3. Roter l'ANCIEN secret (compromis)
- L'ancien client `963294596205-...` avait un secret embarqué dans les versions
  ≥ 1.4.7 déjà publiées → **le roter** dans la console. L'export Drive des anciennes
  versions cessera (attendu) ; elles devront être mises à jour.

### 4. Nettoyer l'historique git AVANT tout push
- Les commits v1.6.1 `3d6d872` et `9131321` contiennent encore `dist/.../drive-export*.js`
  avec le secret dans l'HISTORIQUE → GitHub Push Protection bloquera toujours le push.
- Ces commits ne sont PAS poussés → possibilité de rebase/squash pour retirer `dist` de
  ces commits (ou reset soft jusqu'avant, puis recommit propre sans dist). À faire avec Brice.

### 5. Rebuild + tester dans Brave, puis packager
- `npm run build` (Node 20+), recharger l'extension dans Brave, tester l'export Drive
  (le consentement getAuthToken doit s'ouvrir, l'upload doit réussir).
- Puis `python3 create-package.py` → zip v1.6.2 sans secret → Chrome Web Store.
